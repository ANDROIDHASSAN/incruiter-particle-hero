# Static fallback

Place `hero-static.webp` here — a still render of the brain shape — and swap it
into `StaticHero` in `../ParticleHero.jsx` for the cleanest reduced-motion /
no-WebGL experience. Until then, `StaticHero` shows a CSS-gradient hero with the
same overlay copy and CTA, so the page is never blank or broken.
